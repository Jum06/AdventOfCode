with open("data.txt") as f:
    _input = f.read().splitlines()

def find_max(pack):
    numbers = [0] * 12
    for n in pack:
        break
    return 0


print(sum(find_max(pack) for pack in _input))